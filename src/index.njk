---
layout: base.njk
title: DIArtLOG
---

<h2>Artistic Dialogues with Nature</h2>

<div class="filters">
  <button data-filter="all">Vše</button>
  <button data-filter="krajina">krajina</button>
  <button data-filter="obloha">obloha</button>
  <button data-filter="země">země</button>
  <button data-filter="voda">voda</button>
  <button data-filter="mořský-život">mořský život</button>
  <button data-filter="pigmenty">pigmenty</button>
  <button data-filter="stromy">stromy</button>
  <button data-filter="klimatická-změna">klimatická změna</button>
  <button data-filter="geologie">geologie</button>
  <button data-filter="abstrakce">abstrakce</button>
</div>

<div class="gallery">
  {% for work in works %}
<div class="card" data-tags="{{ work.tags | replace(', ', ',') | replace(',', ' ') }}">
      {% if work.images %}
        {# první obrázek jako náhled #}
        <a href="{{ work.images[0] }}" class="glightbox" data-gallery="work-{{ work.title | slug }}">
          <img src="{{ work.images[0] }}" alt="{{ work.title }}">
        </a>

        {# ostatní obrázky jako skryté odkazy – už se nebudou motat do layoutu #}
        <div style="display:none;">
          {% for img in work.images %}
            {% if not loop.first %}
              <a href="{{ img }}" class="glightbox" data-gallery="work-{{ work.title | slug }}"></a>
            {% endif %}
          {% endfor %}
        </div>

      {% elseif work.video %}
        <video controls>
          <source src="{{ work.video }}" type="video/mp4">
          Váš prohlížeč nepodporuje video tag.
        </video>
      {% endif %}
      
    <p>
  <em>{{ work.title }}</em> | 
  <a href="/authors/{{ work.authorSlug }}/">{{ work.authorName }}</a>
</p>

<p>
  {% if work.yearStart %}
    {{ work.yearStart }}{% if work.yearEnd %}–{{ work.yearEnd }}{% endif %}
  {% else %}
    {{ work.year | trim }}{% endif %}{% if work.technique %} | {{ work.technique | trim }}{% endif %}
</p>







  
      {% if work.description %}
        <div class="description">
          <p>{{ work.description }}</p>
          <button class="toggle-btn">více</button>
        </div>
      {% endif %}

    </div>
  {% endfor %}
</div>
